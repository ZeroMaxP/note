```js
// attr  get first ，set all
$(dom).attr(src:"test.jpg")
offset({
    top:
    left:
})   //会帮你动态计算 让你的top=50 到页面顶部
position( ) //到父级的距离  只能获取
scrollTop()  //向下移动的距离
innerHeight() padding+content  //设置不给padding 获取会算padding
outHeight() //计算padding和content border

```
